// Tample Strings

const name = 'Enoc';
const lastname = 'Hera';


// const completeName = name + ' ' + lastname;
const fullName = `
${name}
${lastname}
`;

console.log(fullName)

export const greeting = ( name ) => {
    return `Hello, ${name}`;
}